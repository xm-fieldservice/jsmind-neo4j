# 数据加载器集成指南

**日期**: 2025-10-07 22:39  
**任务**: Phase 2.1.3 数据加载器集成  
**预计时间**: 5分钟

---

## 📋 需要完成的操作

### 步骤1: 在HTML中添加脚本引用

**文件**: `mindmap-standalone.html`  
**位置**: 第70行后

**添加内容**:
```html
    
    <!-- 🆕 Phase 2.1: 数据加载器模块 -->
    <script src="js/MindmapDataLoader.js"></script>
```

**完整上下文**（第66-73行）:
```html
    <!-- 🆕 Phase 2.1: 内容编辑器模块 -->
    <script src="js/MindmapContentEditor.js"></script>
    
    <!-- 🆕 Phase 2.1: UI管理器模块 -->
    <script src="js/MindmapUIManager.js"></script>
    
    <!-- 🆕 Phase 2.1: 数据加载器模块 -->  ← 添加这3行
    <script src="js/MindmapDataLoader.js"></script>
</head>
```

---

### 步骤2: 在MindmapColumn中初始化数据加载器

**文件**: `mindmap-standalone.html`  
**位置**: 约第385-393行（在UI管理器初始化后）

**添加内容**:
```javascript
                // 🆕 Phase 2.1: 初始化数据加载器
                if (typeof window.MindmapDataLoader !== 'undefined') {
                    this.dataLoader = new window.MindmapDataLoader(
                        this.jm,
                        this.storageCoordinator,
                        this.uiManager,
                        this.errorHandler
                    );
                    console.log('[脑图工作栏] ✅ 数据加载器初始化完成');
                }
```

**完整上下文**:
```javascript
                // 🆕 Phase 2.1: 初始化UI管理器
                if (typeof window.MindmapUIManager !== 'undefined') {
                    this.uiManager = new window.MindmapUIManager(
                        this.jm,
                        this.eventCoordinator
                    );
                    console.log('[脑图工作栏] ✅ UI管理器初始化完成');
                }
                
                // 🆕 Phase 2.1: 初始化数据加载器  ← 添加这11行
                if (typeof window.MindmapDataLoader !== 'undefined') {
                    this.dataLoader = new window.MindmapDataLoader(
                        this.jm,
                        this.storageCoordinator,
                        this.uiManager,
                        this.errorHandler
                    );
                    console.log('[脑图工作栏] ✅ 数据加载器初始化完成');
                }
                
                // 🆕 创建并初始化兼容层
```

---

### 步骤3: 替换loadInitialData调用（可选）

**文件**: `mindmap-standalone.html`  
**位置**: 约第409行

**原代码**:
```javascript
                // 优先从统一存储加载，失败回退到测试数据
                await this.loadInitialData();
```

**替换为**:
```javascript
                // 🆕 Phase 2.1: 使用数据加载器
                if (this.dataLoader) {
                    await this.dataLoader.loadInitialData();
                } else {
                    // 降级：使用原有方法
                    await this.loadInitialData();
                }
```

---

## ✅ 验证步骤

### 1. 打开浏览器控制台
查看是否有以下日志：
```
[DataLoader] 数据加载器初始化完成
[脑图工作栏] ✅ 数据加载器初始化完成
[DataLoader] ✅ 已从存储加载数据
```

### 2. 测试功能
- 刷新页面，数据应正常加载
- 节点计数应正确显示
- 无错误日志

---

## 🎯 完成后提交

```bash
git add .
git commit -m "✅ Phase 2.1.3完成: 数据加载器完全集成

集成MindmapDataLoader到HTML:
- 在HTML中引用MindmapDataLoader.js
- 在MindmapColumn中初始化
- 替换loadInitialData调用

测试验证:
- ✅ 数据加载器正常初始化
- ✅ 数据正常加载
- ✅ 节点计数正确

Phase 2.1进度: 75% → 100%
下一步: Phase 2.1.4 核心控制器"
```

---

**程序员**  
**2025-10-07 22:39**
