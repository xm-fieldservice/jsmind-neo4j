内容：
# 脑图的优化和改造

  - 开发
    - 成功后，导入标签，然后恢复标签同步面板
    - 卡片
      - 脑图卡片的标题做成两行显示
      - New Node
    - 节点
      - 复制节点的时候，粘贴到文本编辑器内的文字，只包含标题+ 内容框内的内容，不包含: 创建时间戳
      - 创建了复制命令的用法
      - 可以实现跨脑图剪切复制节点
    - 详情页
      - 将详情页中“内容框”的全屏按键改造
  - 脑图卡片的优化
    - 所有新建的脑图都默认设置为“收藏”状态。
  - 脑图栏按键的优化
    - 如果把MD文档写入完成，不必要读写按键了
  - 确认两个脑图是否都写入了本地的MD初始库内。
    - 实现MD本地文档的本地存储（即时），并且保留好与服务器MD文档同步的接口和规范约定
  - 增加脑图系统的选择器
  - 开发系统选择器代码：手机和PC，服务器
  - ，内容框全屏模式下，内容框点击会话列表，全屏框内只显示该会话内容。