# 脑图系统缓存机制完整梳理

## 🔍 **缓存层级架构**

### 1. **文件存储层** (最底层 - 数据源)
```
data/
├── all_mindmaps.json          # 主数据文件 ✅ 已恢复20个节点
├── mindmap_registry.json      # 注册表备份
└── unified_mindmap_storage.md # MD底座文件
```

### 2. **localStorage缓存层** (浏览器持久化)
```javascript
// 核心缓存键
'mindmap_data_v1'              // 当前脑图数据 (主缓存)
'__mind_full_cache_v1'         // 全图快照缓存
'mm_project_catalog_v1'        // 项目目录缓存

// 动态缓存键 (按脑图ID生成)
'mm:root-1758256084936-b8:data' // 特定脑图数据
'mm:root-1758256084936-b8:meta' // 特定脑图元数据
```

### 3. **内存缓存层** (运行时)
```javascript
window.__mindFullCache         // 全局内存缓存 ⚠️ 问题源头
window.mindmapController.data  // 控制器内存数据
window.mindmapController.mind  // jsMind实例数据
```

## 🚨 **缓存冲突机制分析**

### 问题1: **多重缓存覆盖循环**
```
用户操作 → 内存更新 → localStorage写入 → 文件同步 → 重新加载 → 覆盖内存
     ↑                                                                    ↓
     ←←←←←←←←←←←←←←← 系统检测变化 ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←
```

### 问题2: **`__mindFullCache` 持续重置**
从日志看到每5秒重复警告：
```
[调试] 🚨 检测到潜在问题: {"issues":["⚠️ __mindFullCache 仍然存在，可能有代码在设置它"]}
```

**根因分析**：
- `script.js:1051` - 初始化 `window.__mindFullCache = null`
- `script.js:1065` - 条件设置 `window.__mindFullCache = snap`
- `script.js:1139` - 读取时回退 `window.__mindFullCache || null`
- `script.js:1203` - 聚焦时备份 `window.__mindFullCache = snap0`
- `script.js:1451` - 显示时设置 `window.__mindFullCache = snap`
- `script.js:1711` - 切换时更新 `window.__mindFullCache = targetPack`

**问题**：至少6个地方在操作这个全局变量！

### 问题3: **自动同步机制冲突**
```javascript
// 多个定时器同时运行
UnifiedStorageManager.syncToJsonBase()  // 每5分钟同步
MindmapController.autoSave()           // 实时保存
Registry.periodicSync()                // 定期同步注册表
DebugPanel.monitor()                   // 每5秒检查
```

## 🔧 **缓存数据流向图**

```
文件层: all_mindmaps.json (✅ 25个节点)
   ↓ 加载
localStorage: mindmap_data_v1 (❓ 可能是旧数据)
   ↓ 读取
内存层: window.__mindFullCache (⚠️ 被多处重置)
   ↓ 渲染
显示层: jsMind实例 (❌ 只显示5个节点)
```

**问题**：数据在每一层都可能被不同的机制覆盖！

## 🎯 **具体的缓存操作点**

### A. **数据加载路径**
1. `MindmapController.loadFromStorage()` 
   - 优先读取 `mm:{mindKey}:data`
   - 回退到 `mindmap_data_v1`
   - 最后从文件加载

2. `script.js` 全图缓存逻辑
   - `tryInitFullCache()` - 初始化缓存
   - `restoreFullMind()` - 恢复全图
   - 多处设置 `__mindFullCache`

### B. **数据保存路径**
1. `MindmapController.saveMindmapToStorage()`
   - 保存到 `mm:{mindKey}:data`
   - 同步到 `mindmap_data_v1`
   - 触发文件同步

2. `UnifiedStorageManager.saveMindmapData()`
   - 立即保存到localStorage
   - 延迟同步到JSON文件
   - 标记dirty状态

### C. **缓存清理机制**
1. `UnifiedStorageManager.cleanExpiredCache()`
   - 清理过期的关系缓存
   - 清理过期的脑图缓存
   - 但不清理核心缓存

2. 手动清理（我们的工具）
   - 只清理明显的冗余数据
   - 保留核心缓存键

## 🚨 **根本问题诊断**

### 问题根源：**缓存一致性缺失**
1. **文件层** ✅ 数据正确 (25个节点)
2. **localStorage层** ❓ 可能不一致
3. **内存层** ⚠️ 被多处重置
4. **显示层** ❌ 显示错误 (只有5个节点)

### 关键冲突点：
```javascript
// jsmind-controller.js:703 - 获取mindKey时的逻辑
const mk = (function(){
    try{ 
        const p = window.__mindFullCache; 
        const mid = p && p.meta && p.meta.mind_id; 
        if (mid) return String(mid); 
    }catch(_){ }
    // ... 其他逻辑
}).call(this);
```

**问题**：mindKey的解析依赖 `__mindFullCache`，但这个变量被多处修改！

## 💡 **解决方案架构**

### 方案1: **缓存隔离** (推荐)
```javascript
// 禁用全局缓存变量
delete window.__mindFullCache;

// 使用独立的缓存管理器
class IsolatedCacheManager {
    constructor() {
        this.cache = new Map();
        this.locked = false;
    }
    
    set(key, value) {
        if (!this.locked) {
            this.cache.set(key, value);
        }
    }
    
    lock() { this.locked = true; }
    unlock() { this.locked = false; }
}
```

### 方案2: **单向数据流** (简单)
```
文件 → localStorage → 内存 → 显示
(禁止反向同步，避免循环)
```

### 方案3: **强制刷新** (当前)
```javascript
// 清除所有缓存层
localStorage.clear();
delete window.__mindFullCache;
window.location.reload(true);
```

## 🔄 **当前状态总结**

### ✅ **已解决**：
- 数据层恢复：20个节点已写入 `all_mindmaps.json`
- 缓存分析：识别了6个 `__mindFullCache` 操作点
- 冲突定位：找到了多重缓存覆盖的根因

### ⚠️ **待解决**：
- 浏览器显示：需要强制刷新缓存
- 架构优化：需要重构缓存机制
- 一致性保证：需要建立缓存锁机制

### 🎯 **下一步**：
1. **立即**：使用 `force_reload.html` 强制刷新
2. **短期**：实现缓存隔离机制
3. **长期**：重构为单向数据流架构

这就是为什么简单的localStorage清理没有效果的原因 - 问题在于**缓存架构的复杂性**，而不是数据量的问题。
