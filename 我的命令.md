# 项目启动命令

## 标准启动模式
```bash
# 启动完整系统（推荐）
python tools/start.py --full

# 仅启动后端
python tools/start.py

# 仅启动前端
python tools/start.py --frontend
```

## Neo4j + D3.js 集成模式
```bash
# 检查Neo4j配置（推荐先运行）
python tools/check_neo4j_config.py

# 启动Neo4j关系图可视化集成（包含JSON底座API）
python tools/start.py --neo4j

# 测试Neo4j集成功能
python test_d3_neo4j_integration.py
```

## 🆕 关系图可视化功能
```bash
# 标准启动（现已包含关系图功能）
python tools/start.py --full

# 服务端口说明：
# - 前端: http://localhost:8082
# - 后端API: http://localhost:8081  
# - JSON底座API: http://localhost:5001
# - Neo4j后端: http://localhost:8000 (仅--neo4j模式)
```

## 其他选项
```bash
# 自定义端口
python tools/start.py --port 8090 --frontend-port 3000

# 不自动打开浏览器
python tools/start.py --full --no-browser
```

